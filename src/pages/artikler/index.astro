---
import { getCollection } from "astro:content";
const posts = await getCollection("articles"); // endret fra "artikler"
const sorted = posts
  .filter(p => p.data.pubDate)
  .sort((a,b) => new Date(b.data.pubDate).getTime() - new Date(a.data.pubDate).getTime());
---
<!-- render … bruk sorted -->
