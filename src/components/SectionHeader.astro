---
const { title, kicker } = Astro.props;
---
<div class="mx-auto max-w-[var(--container)] px-4 sm:px-6 mt-8 mb-4">
  <p class="text-lupen-accent font-semibold">{kicker}</p>
  <h2 class="text-2xl sm:text-3xl font-bold" style="font-family: 'Space Grotesk', sans-serif;">{title}</h2>
</div>
